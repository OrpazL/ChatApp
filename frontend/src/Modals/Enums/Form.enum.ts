export enum EnumInputType {
    Input,
    Textarea,
    Select,
    CheckBox,
    AutoComplete
}
export enum ValidationEnum {
    required = 'required',
    minLength = 'minLength',
    maxLength = 'maxLength',
}
